

/*export const addBook = (title, author) => {
    return (
        dispatch({
        type: 'ADD_BOOK',
        book: {
            title: title,
            author: author,
        }})
    )
}

export const removeBook = (id) => {
    return (
        dispatch({
            type: 'REMOVE_BOOK',
            id: id
        })
    )
}*/